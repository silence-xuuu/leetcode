bool isUgly(int num){
    if(num==0) return false;
    while(num!=1){
        int count=num;
        while(count%2==0) count/=2;
        while(count%3==0) count/=3;
        while(count%5==0) count/=5;
        if(count==num) return false;
        else num=count;
    }
    return true;
}

思路：
若为零直接输出不是；若不等于一则分别对2,3,5取商直到等于一跳出循环或都无法除尽返回错误。
