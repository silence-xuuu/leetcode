/*
斐波那契数，通常用 F(n) 表示，形成的序列称为斐波那契数列。该数列由 0 和 1 开始，后面的每一项数字都是前面两项数字的和。也就是：

F(0) = 0,   F(1) = 1
F(N) = F(N - 1) + F(N - 2), 其中 N > 1.
给定 N，计算 F(N)。
*/

int fib(int N){
    int a=0,b=1,count=0,i;
    if(N==1) return 1;
    if(N==2) return 1;
    for(i=2;i<=N;i++){
        count=a+b;
        a=b;
        b=count;
    }
    return count;
}

/*
0ms 100%

思路：迭代+特殊情况处理。

思考感悟：
为减少时间的消耗使用迭代法减少计算的次数。
一定注意有关多次计算的题目不能使用递归法。
*/
