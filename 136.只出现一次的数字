/*
给定一个非空整数数组，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。

输入: [2,2,1]
输出: 1
*/

int singleNumber(int* nums, int numsSize){
    int temp=0;
    for(int i=0;i<numsSize;i++){
        temp^=nums[i];
    }
    return temp;
}

/*
12ms 98.05%
8.1mb 5.20%

思路：
通过亦或的结合律和交换律，当0与所有数分别亦或时，相同的数字将会抵消，而剩下的就是只出现了一次的数字

思考感悟：
1.此题由于是求没有出现过两遍的数字，很容易第一反应想到哈希表，但由于时间复杂度超标无法通过。
2.亦或操作的巧妙使用可以在数组元素出现偶数遍时将其抵消，而在另外的一些题目中，亦或同样可以在元素的消去中发挥作用（389题实例）。
*/
